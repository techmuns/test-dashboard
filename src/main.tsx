import React from "react";
import ReactDOM from "react-dom/client";
import { DashboardChatWebReader } from "./dashboard_chat_web_reader";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <DashboardChatWebReader />
  </React.StrictMode>,
);
